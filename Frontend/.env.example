# URL of your running backend's API root (the Express app mounts routes under /api).
# Must match what your backend's CORS "origin" allows — the backend in this repo is
# hardcoded to allow http://localhost:5173 (Vite's default dev port), so keep the
# frontend on that port during development.
VITE_API_URL=http://localhost:3000/api
