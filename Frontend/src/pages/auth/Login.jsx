import { useState } from 'react'; import { Link } from 'react-router-dom'; import { Landmark, Eye, Lock, Mail, ArrowRight, Loader2, AlertCircle } from 'lucide-react'; import { useAuth } from '../../context/AuthContext'; export default function Login() { const { login } = useAuth(); const [showPassword, setShowPassword] = useState(false); const [isLoading, setIsLoading] = useState(false); const [error, setError] = useState(''); const [formData, setFormData] = useState({ email: '', password: '' }); const handleChange = (e) => setFormData(prev => ({ ...prev, [e.target.name]: e.target.value })); const handleSubmit = async (e) => { e.preventDefault(); setError(''); setIsLoading(true); const res = await login(formData.email, formData.password); setIsLoading(false); if (!res.success) setError(res.error); }; return (<div className="w-full flex h-screen bg-background"><div className="hidden lg:flex w-1/2 flex-col justify-between p-12 relative overflow-hidden bg-[#0A0A0E]"><div className="relative z-10"><div className="flex items-center gap-3 text-white mb-20"><Landmark size={24} /><span className="font-semibold text-xl tracking-wide">Finora</span></div><h1 className="text-5xl font-bold text-white mb-6 leading-tight">Financial clarity,<br />redefined.</h1><p className="text-zinc-400 text-lg max-w-md">Enter a sophisticated ecosystem designed for precision wealth management and uncompromising security.</p></div><div className="absolute bottom-0 left-0 w-full h-[400px] pointer-events-none"><div className="absolute bottom-0 w-full h-full bg-gradient-to-t from-primary/10 to-transparent"></div><svg viewBox="0 0 500 200" className="absolute bottom-10 w-full h-auto stroke-primary stroke-2 fill-none drop-shadow-[0_0_15px_rgba(124,58,237,0.5)]"><path d="M 0 150 C 100 150, 150 160, 200 130 C 250 100, 300 80, 400 60 C 450 50, 480 30, 500 20" /><circle cx="200" cy="130" r="4" fill="#fff" className="stroke-none" /><circle cx="400" cy="60" r="4" fill="#fff" className="stroke-none" /></svg></div></div><div className="w-full lg:w-1/2 flex flex-col justify-center items-center p-6 sm:p-8 bg-[#101014]"><div className="w-full max-w-md"><div className="mb-10 text-center lg:text-left"><h2 className="text-3xl sm:text-4xl font-bold text-white mb-3 tracking-tight">Welcome back</h2><p className="text-zinc-400">Log in to your account to continue.</p></div><form onSubmit={handleSubmit} className="bg-card border border-border rounded-2xl p-6 shadow-xl">{error && <div className="bg-rose-500/10 border border-rose-500/20 text-rose-500 p-3 rounded-lg text-sm mb-5 flex items-center gap-2"><AlertCircle size={16} />{error}</div>}<div className="space-y-5"><div><label className="block text-xs font-medium text-zinc-400 mb-2">Email</label><div className="relative"><Mail className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-500" size={16} /><input type="email" name="email" value={formData.email} onChange={handleChange} className="input-field pl-10" required disabled={isLoading}/></div></div><div><div className="flex justify-between items-center mb-2"><label className="block text-xs font-medium text-zinc-400">Password</label></div><div className="relative"><Lock className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-500" size={16} /><input type={showPassword ? "text" : "password"} name="password" value={formData.password} onChange={handleChange} className="input-field pl-10 pr-10" required disabled={isLoading}/><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-1/2 -translate-y-1/2 text-zinc-500 hover:text-zinc-300" aria-label="Toggle password visibility"><Eye size={16} /></button></div></div><button type="submit" disabled={isLoading} className="btn-primary mt-6 group">{isLoading ? <Loader2 size={16} className="animate-spin" /> : <>Login <ArrowRight size={16} className="group-hover:translate-x-1 transition-transform" /></>}</button></div></form><div className="mt-8 text-center text-sm text-zinc-400">Don't have an account? <Link to="/register" className="text-white hover:text-primary transition-colors font-medium">Create account</Link></div></div></div></div>); }
