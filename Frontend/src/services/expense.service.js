import api from './api'; export const expenseService = { getExpenses: async (params) => (await api.get('/expenses', { params })).data, getExpense: async (id) => (await api.get(`/expenses/${id}`)).data, createExpense: async (data) => (await api.post('/expenses', data)).data, updateExpense: async (id, data) => (await api.put(`/expenses/${id}`, data)).data, deleteExpense: async (id) => (await api.delete(`/expenses/${id}`)).data };
