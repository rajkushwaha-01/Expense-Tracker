import api from './api'; export const dashboardService = { getDashboardData: async () => { const [analytics, expenses, income, accounts, recurring] = await Promise.all([ api.get('/analytics').catch(() => ({ data: {} })), api.get('/expenses?limit=5').catch(() => ({ data: [] })), api.get('/income').catch(() => ({ data: [] })), api.get('/accounts').catch(() => ({ data: [] })), api.get('/recurring').catch(() => ({ data: [] })) ]); return { analytics: analytics.data, expenses: expenses.data, income: income.data, accounts: accounts.data, recurring: recurring.data }; } };
